# ✅ Recommended way (Node.js 18 LTS – stable)
## 1️⃣ Update packages
```
sudo apt update
```
2️⃣ Install Node.js 18 + npm (official NodeSource)
```
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```
3️⃣ Verify installation
```
node -v
npm -v
```

